services:
  - type: worker
    name: startup-os-dashboard
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python dashboard.py --loop
    envVars:
      - key: PYTHONUNBUFFERED
        value: "1"
